fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-jose[cryptography]==3.4.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.31
email-validator==2.2.0
pytest==8.3.4
httpx==0.28.1
langchain-groq==0.2.1
apscheduler==3.11.3
# R3-04: Redis-backed rate limiter (app/core/utils.py::RedisRateLimiter) and
# Prometheus /metrics endpoint (app/core/metrics.py). redis is imported
# lazily and only used when REDIS_URL is set - installing it here does not
# require a Redis server to be running.
redis==8.1.0
prometheus_client==0.26.0
# Test-only: an in-process fake Redis so tests/test_observability.py can
# exercise RedisRateLimiter's real logic without a running Redis server.
fakeredis==2.37.1
# R3-02: TOTP MFA (app/core/mfa.py).
pyotp==2.10.0
# R1-11/T-04: PDF export (app/services/principal_records.py's principal
# self-service export, and app/api/routes/consents.py's staff export, whose
# `from fpdf import FPDF` had no pin here at all before this).
fpdf2==2.8.2
